# 定时触发器说明

> 官方热搜归纳。`package.json` 内已配置 triggers；**测试 → 生产** 后请在控制台核对是否生效。

## Cron 表达式（已写在 package.json）

```
0 0 */2 * * * *
```

| 含义 | 每 **2** 小时整点执行 |
| 作用 | 根据搜索记录归纳热搜数据 |

## 建议超时 / 内存

| 项 | 建议值 |
|----|--------|
| 超时 | 默认即可（不足可调到 60s～120s） |
| 并发 | 1 |
