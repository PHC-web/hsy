# 定时触发器说明

> **阿里云**控制台格式：字符串数组 + `cron:` 前缀 + **6** 段 Cron（无「年」）。

## 阿里云控制台应填写

```json
["cron:0 */5 * * * *"]
```

| 含义 | 每 **5** 分钟 |

## 建议超时

**≥ 120s**｜内存 512MB｜并发 1

## 作用

`merchant.withdrawSyncProcessing`：日间同步微信已 SUCCESS 提现到本地已到账。

## 文档

https://doc.dcloud.net.cn/uniCloud/trigger.html
